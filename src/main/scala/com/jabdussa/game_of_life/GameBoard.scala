package com.jabdussa.game_of_life

class GameBoard(cells:List[Cell]) {

	def paint = ???

	/*

  val board = Array.ofDim[Int](10, 10)

  for (b <- board) {
    if (x.neighbors < 2) x.die
    if (x.neighbors > 3) x.die
  }

  */

}
